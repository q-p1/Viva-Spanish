{
  "name": "viva-spanish",
  "version": "0.7.0",
  "private": true,
  "description": "Adaptive Spanish learning world with personal memory modeling, Madrid simulation, missions, and multimodal labs.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-standalone.js",
    "check": "npm test && npm run build",
    "serve": "python3 -m http.server 4173"
  },
  "engines": {"node": ">=20"}
}